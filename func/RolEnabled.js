const RolEnabled = (rol, bot, member) => {

     let rolpos = rol.position;
     let botpos = bot.roles.highest.position;
     let memberpos = member.roles.highest.position;

}

module.exports = RolEnabled;